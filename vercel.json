{
  "version": 2,
  "env": {
    "NODE_ENV": "development"
  },
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "./node_modules/.bin/nest build api",
  "outputDirectory": "apps/api/dist",
  "framework": "nestjs"
}
