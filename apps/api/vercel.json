{
  "version": 2,
  "buildCommand": "pnpm install --no-frozen-lockfile && npx prisma generate --schema=../../prisma/schema.prisma && pnpm run build",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/main.js"
    }
  ]
}
